// Program to test If, Else if, and Else statements
STORE 1 IN b

if(1>1) STARTIF
    STORE 2 IN b
ENDIF
elif(1>2) STARTELIF
    STORE 3 IN b
ENDELIF
STARTELSE
    STORE 4 IN b
ENDELSE

show(b)

STORE 1 IN c
if(b>1) STARTIF
  STORE 2 IN c
ENDIF
STARTELSE
  STORE 3 IN c
ENDELSE
show(c)

FUNCTION someRandonFunc() STARTFUNCDEF
  STORE 1 IN c
ENDFUNCDEF

someRandomFunc()