// This is a comment

STORE (1+12)*2 IN init
STORE init+7 IN cuh
STORE 1 IN b
STORE 2 IN a

show("Init before if statement:")
show(init)

show("B before if statement: ")
show(b)

if(1>0) STARTIF
    STORE cuh IN init
ENDIF
elif(1>0) STARTELIF
    STORE a IN b
    STORE 1 IN b
ENDELIF

show("Init after if statement: ")
show(init)


show("B after if statement: ")
show(b)